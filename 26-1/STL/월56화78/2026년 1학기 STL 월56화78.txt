//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

int main()
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostreambuf_iterator<char>{out});
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

int main()
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostreambuf_iterator<char>{out});
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	//std::copy(std::istreambuf_iterator<char>{in}, {},
	//	std::ostreambuf_iterator<char>{out});
	char c;
	in >> std::noskipws;
	while (in >> c)
		out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;
}﻿//-------------------------------------------------------------------//2026년1학기STL월56화783월3일(1주1일)//-------------------------------------------------------------------//컴파일환경-Release/x64//VS버전-17.14.27//프로젝트설정-C++언어표준-/std:c++lastest//-C/C++언어-SDL검사-아니요//-------------------------------------------------------------------//-------------------------------------------------------------------#include<iostream>#include<string>#include<fstream>#include<algorithm>voidsave(conststd::string&);//사용하지말아야할자료형->대체할수있다.->zero-overhead:대체품을사용함으로써생기는불리함이없음//1.char*->std::string//char*name="2026년";//name글자수?????->모름//strlen(name);->외부의함수를이용해서만글자수확인가능//std::cout<<"글자수-"<<fname.length()<<'\n';//2.T*->smartpointer를사용할것//3.T[]->std::array를사용할것//--------intmain()//--------{std::cout<<"2026년3월3일"<<'\n';save("메인.cpp");}//namingconvension->변수이름작명법참고voidsave(conststd::string&fname){std::ifstreamin{fname};//RAII공부할것//fname파일이존재하는지확인if(notin){//!는읽기어렵기때문에not으로바꿔쓸것std::cout<<fname<<"-열수없습니다."<<'\n';exit(202630);}std::ofstreamout{"2026년1학기STL월56화78.txt",std::ios::app};//std::ios::app이전내용에추가하는코드//std::copy(어디에서,어디까지를,어디로);std::copy(std::istream_iterator<char>{in},{},std::ostream_iterator<char>{out});//charc;//in>>std::noskipws;//while(in>>c)//out<<c;//operator>>(in,c);//고수준입력명령어//std::cout<<c;}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});

	char c;
	in >> std::noskipws;
	while (in >> c)
		out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:22:27.9791166
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:24:25.3456257
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - %Y  -%m  -%d   %H   %M   %S  
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << std::format("%Y년-%m월-%d일 %H시 %M분 %S분", now + 9h) << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - %Y  -%m  -%d   %H   %M   %S  
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << std::format("%Y년-%m월-%d일 %H시 %M분 %S분", now + 9h) << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:34:54.4559081
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:48:55.2395772
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:52:25.9764022
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:53:00.6643528
=====================================================================

//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------
#pragma once

void save(const std::string&);

=====================================================================
        ð  - 2026-03-09 14:53:00.6657703
=====================================================================

﻿//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <chrono>
#include "save.h"

void save(const std::string& fname)
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;

	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:53:00.6670266
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:55:26.0457619
=====================================================================

//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------
#pragma once

void save(const std::string&);

=====================================================================
        ð  - 2026-03-09 14:55:26.0468581
=====================================================================

﻿//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <chrono>
#include "save.h"

void save(const std::string& fname)
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;

	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:55:26.0477932
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:58:44.5254920
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int* a, int* b);

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 15:07:28.5541600
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int* a, int* b);

//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

//
// 메모리
// 
//


=====================================================================
        ð  - 2026-03-09 15:08:19.0783693
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int* a, int* b);

int a{ 1 };
//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int  b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

//
// 메모리
// 
//


=====================================================================
        ð  - 2026-03-09 15:11:56.1215318
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int*, int*);

//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp{ *a };
	*a = *b;
	*b = tmp;
}

=====================================================================
        ð  - 2026-03-09 15:12:29.6218516
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int*, int*);

//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int temp{ *a };
	*a = *b;
	*b = temp;
}

=====================================================================
        ð  - 2026-03-09 15:12:53.0890023
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int*, int*);

//--------
int main()
//--------
{	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int temp{ *a };
	*a = *b;
	*b = temp;
}

=====================================================================
        ð  - 2026-03-09 15:14:05.6198487
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int&, int&);

//--------
int main()
//--------
{	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(a, b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

=====================================================================
Save Time - 2026-03-09 16:47:45.6190340
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

class Dog {
private:
	int num;
public:
	Dog(int i) {
		num = i;
	}

	operator int&() {
		return num;
	}
};

void change(int&, int&);

//--------
int main()
//--------
{
	// [문제] 의도대로 실행되게 하자
	{
		// using Dog = int; , #define Dog Int 사용하지 말기
		Dog a{ 1 }, b{ 2 }; // 살려보기, Dog는 클래스, 템플릿 공부하기
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

=====================================================================
Save Time - 2026-03-10 15:43:11.4496064
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

class Dog; //

void change(int&, int&);
void change(Dog&, Dog&);

// 내가 쓴 답, 잘 작동 됨
//class Dog {
//private:
//	int num;
//public:
//	Dog(int i) {
//		num = i;
//	}
//
//	operator int&() {
//		return num;
//	}
//};

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {}

	operator int() {
		return num;
	}
private:
	int num;
};

//--------
int main()
//--------
{
	// [문제] 의도대로 실행되게 하자
	{
		// using Dog = int; , #define Dog Int 사용하지 말기
		Dog a{ 1 }, b{ 2 }; // 살려보기, Dog는 클래스, 템플릿 공부하기
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b)
{
	Dog temp{ a };
	a = b; // a.orperator = (b);
	b = temp;
}

=====================================================================
Save Time - 2026-03-10 16:06:09.7780263
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

class Dog; //

void change(int&, int&);
void change(Dog&, Dog&);

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {}

	// 이 코드는 사용하지 말자
	//operator int() {
	//	return num;
	//}

private:
	int num;

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

//--------
int main()
//--------
{
	// [문제] 의도대로 실행되게 하자
	{
		// using Dog = int; , #define Dog Int 사용하지 말기
		Dog a{ 1 }, b{ 2 }; // 살려보기, Dog는 클래스, 템플릿 공부하기
		// Dog a = 1, b = 2; -> Dog a = Dog { (int) 1 }; 강제로 형변환 시킴, 위험할 수 있음
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b)
{
	Dog temp{ a };
	a = b; // a.orperator = (b);
	b = temp;
}

=====================================================================
Save Time - 2026-03-10 16:46:41.3458408
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a, b;
	change(a, b);

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:47:09.7757364
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a, b;
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:47:32.3454679
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a = 1, b = 2;
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:47:47.8966570
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:48:45.6010671
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:52:18.8578401
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
	std::cout << "찍어낸 코드" << std::endl;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;
	
	std::string S{ "2026년" }, T{ "3월10일" };
	change(S, T);
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:52:36.0851529
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
	std::cout << "찍어낸 코드" << std::endl;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;
	
	std::string S{ "2026년" }, T{ "3월10일" };
	change(S, T);
	std::cout << S << ' ' << T << std::endl;
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:54:00.9990394
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	std::cout << "이 문장을 보면 컴파일러가 열일한 줄 알아라." << '\n';
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
	std::cout << "찍어낸 코드" << std::endl;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;
	
	std::string S{ "2026년" }, T{ "3월10일" };
	change(S, T);
	std::cout << S << ' ' << T << std::endl;
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:06:45.0328923
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(T a, T b)
{
	return a + b;
}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년"), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:07:23.5788312
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(T a, T b)
{
	return a + b;
}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년 "), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:12:17.8282934
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;
}

//template <>
//std::string add<std::string>(const std::string& a, const std::string& b)
//{
//	return std::string(a + b); // RVO -> 따로 보기
//}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년 "), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:13:41.1508646
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;
}

template <>
std::string add<std::string>(const std::string& a, const std::string& b)
{
	return std::string(a + b); // RVO -> 따로 보기
}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년 "), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:16:53.4370726
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:21:10.6356808
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:27:55.7170371
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:38:41.5939756
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid;

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 100; i++) {
		std::cout << uid(dre) << std::endl;;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:40:35.7202808
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 100; i++) {
		std::cout << uid(dre) << std::endl;;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:41:16.5987185
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::cout << uid(dre) << std::endl;;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:42:57.2020053
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::print("{:8}", uid(dre));
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:43:12.7404450
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::print("{:8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:43:38.6401403
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::print("{:<8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:48:37.8633222
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a)
	{
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", a);
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:48:49.9016565
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a)
	{
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", a);
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:51:02.2262098
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", a);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:51:31.8715058
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<10}", a);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:51:52.9897612
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:8}", a);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:27.0804536
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:32.6866039
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:39.6720049
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:43.9988861
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:53:14.6878575
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max{ 0 };
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:53:41.5644584
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max{ 0 };
	for (int& num : a) {
		if (num > max) {
			max = num;
		}
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:54:32.4079360
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max{ 0 };
	for (int& num : a) {
		if (num > max) {
			max = num;
		}
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:02:01.9583160
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int maxVal{ std::numeric_limits<int>::min() };
	for (int& num : a) {
		if (maxVal < num) {
			maxVal = num;
		}
	}
	std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:02:15.7446557
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int maxVal{ std::numeric_limits<int>::min() };
	for (int& num : a) {
		if (maxVal < num) {
			maxVal = num;
		}
	}

	std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:02:19.7686681
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int maxVal{ std::numeric_limits<int>::min() };
	for (int& num : a) {
		if (maxVal < num) {
			maxVal = num;
		}
	}

	std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:15:03.7011477
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	// 이미 정의되어 있는 함수를 굳이 코딩할 필요가 없음
	//int maxVal{ std::numeric_limits<int>::min() };
	//for (int& num : a) {
	//	if (maxVal < num) {
	//		maxVal = num;
	//	}
	//}
	//std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	// 0점 자리 답
	//std::sort(std::begin(a), std::end(a));
	//std::cout << "가장 큰 값 - " << a[1000 - 1] << std::endl;

	// std::cout << "가장 큰 값 -" << std::max_element(a) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:19:59.7791069
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	int* p = std::max_element(std::begin(a), std::end(a)); // begin(a) == &a[0], end(a) == &a[1000]
	std::cout << "가장 큰 값 - " << *p << std::endl; // *p 역참조 연산자 dereference

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:24:04.4154538
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	int* p = std::max_element(std::begin(a), std::end(a)); // begin(a) == &a[0], end(a) == &a[1000] -> 하드 와이어드 코딩, 코드 수정 시 오류날 가능성이 생겨서 추천 안함
	std::cout << "가장 큰 값 - " << *p << std::endl; // *p 역참조 연산자 dereference

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:40:55.1834226
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ std::random_device{}()};
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	int* p = std::max_element(std::begin(a), std::end(a)); // begin(a) == &a[0], end(a) == &a[1000] -> 하드 와이어드 코딩, 코드 수정 시 오류날 가능성이 생겨서 추천 안함
	std::cout << "가장 큰 값 - " << *p << std::endl; // *p 역참조 연산자 dereference

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:48:47.6783610
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	int* maxVal{ std::max_element(std::begin(a), std::end(a)) };
	int* minVal{ std::min_element(std::begin(a), std::end(a)) };

	std::cout << "가장 작은 값 - " << *minVal << " 가장 큰 값 - " << *maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:56:52.9196951
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << res.first << " 가장 큰 값 - " << res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:57:06.8044094
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *res.first << " 가장 큰 값 - " << *res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:57:27.0679188
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(res.first) << " 가장 큰 값 - " << *(res.second) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:57:48.2672523
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(res.first) << " 가장 큰 값 - " << *(res.second) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:01:39.0029019
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	auto [minVal, maxVal] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << minVal << " 가장 큰 값 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:01:56.9564532
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	auto [minVal, maxVal] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(minVal) << " 가장 큰 값 - " << *(maxVal) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:09:16.6061794
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int [1000] 이런 자료구조는 앞으로는 쓰지 말 것, (new int[1000]; 제외)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// array<T,N>
	
	//int a[1000]; // 자료형은 int[N]인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	auto [minVal, maxVal] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(minVal) << " 가장 큰 값 - " << *(maxVal) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:10:26.4816136
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int [1000] 이런 자료구조는 앞으로는 쓰지 말 것, (new int[1000]; 제외)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// array<T,N>
	
	//int a[1000]; // 자료형은 int[N]인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	auto [minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:11:20.2966708
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
		std::print("{:8}", num);
	}

	auto [minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:12:50.6546852
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	std::array<int, 1000> a;
	std::cout << "a가 사용하는 메모리 - " << sizeof a << std::endl;

	for (int& num : a) {
		num = uid(dre);
		std::print("{:8}", num);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:13:11.4952251
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	std::array<int, 1000> a;
	std::cout << "a가 사용하는 메모리 - " << sizeof a << std::endl;

	for (int& num : a) {
		num = uid(dre);
		std::print("{:8}", num);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:19:59.1551807
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

template <class T, int N>
class myArray {
public:
	size_t size() const{
		return N;
	}

	int& operator[](int idx) {
		return a[idx];
	}
	T* begin() {
		return &a[0];
	}
	T* end() {
		return &a[N];
	}

private:
	T a[N];
};

//--------
int main()
//--------
{
	myArray<int, 1000> a;

	//for (int& num : a) {
	//	num = uid(dre);
	//	std::print("{:8}", num);
	//}

	for (int i = 0; i < a.size(); ++i) {
		a[i] = uid(dre);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:20:27.5968804
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

template <class T, int N>
class myArray {
public:
	size_t size() const{
		return N;
	}

	int& operator[](int idx) {
		return a[idx];
	}
	T* begin() {
		return &a[0];
	}
	T* end() {
		return &a[N];
	}

private:
	T a[N];
};

//--------
int main()
//--------
{
	myArray<int, 1000> a;

	//for (int& num : a) {
	//	num = uid(dre);
	//	std::print("{:8}", num);
	//}

	for (int i = 0; i < a.size(); ++i) {
		a[i] = uid(dre);
		std::print("{:8}", a[i]);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 15:54:42.1336413
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:02:17.8048984
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:02:57.0603632
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << " ";
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:05:03.5589246
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 1000'0000; ++i) {
		out << uid(dre) << std::endl;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:07:14.2638526
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 100; ++i) { // 119818581 byte
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:08:20.4290099
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 1000'0000; ++i) { // 119818581 byte
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:40:01.1088842
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include <fstream>
#include "save.h"

std::array<int, 1000'1000> a;

//--------
int main()
//--------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자.
	// 가장 작은 값을 찾아 출력하라.
	
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요." << std::endl;
		return 20260317;
	}

	for (int& num : a) {
		in >> num;
	}

	std::cout << *(std::min_element(a.begin(), a.end()));
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:52:32.1271913
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <algorithm>
#include <fstream>
#include "save.h"

//--------
int main()
//--------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자.
	// 가장 작은 값을 찾아 출력하라.
	
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요." << std::endl;
		return 20260317;
	}

	std::cout << "최솟값 - " << *(std::min_element(std::istream_iterator<int>{in}, {})) << std::endl;
	// stream 데이터의 흐름, 

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:12:49.2920908
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}


	//std::pair <int, int> res = std::minmax_element();

	//std::cout << "최솟값 - " << res.first << " 최댓값 - " << res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:20:11.3292195
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

std::array<int, 10000000> a;

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}

	in.read((char*)(a.data()), a.size() * sizeof(int));

	auto [minVal, maxVal] = std::minmax_element(a.begin(), a.end());

	std::cout << "최솟값 - " << *minVal << " 최댓값 - " << *maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:12:49.2920908
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}


	//std::pair <int, int> res = std::minmax_element();

	//std::cout << "최솟값 - " << res.first << " 최댓값 - " << res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:20:11.3292195
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

std::array<int, 10000000> a;

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}

	in.read((char*)(a.data()), a.size() * sizeof(int));

	auto [minVal, maxVal] = std::minmax_element(a.begin(), a.end());

	std::cout << "최솟값 - " << *minVal << " 최댓값 - " << *maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:16:53.4370726
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:21:10.6356808
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:46:42.8946179
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;			// [0, 9999]

	friend std::ostream& operator << (std::ostream & os, const Dog & dog) {
		print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{
	Dog dog;

	std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:48:17.4941733
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;			// [0, 9999]

	friend std::ostream& operator << (std::ostream & os, const Dog & dog) {
		print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{
	std::array <Dog, 1000> dogs;
	
	for (Dog dog : dogs)
	{
		std::cout << dog << std::endl;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:54:33.0096161
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;			// [0, 9999]

	friend std::ostream& operator << (std::ostream & os, const Dog & dog) {
		print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{
	std::array <Dog, 1000> dogs;
	
	std::ofstream out{ "Dog천마리", std::ios::binary };
	out.write((char*)(dogs.data()), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:59:02.4700953
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"

class Dog {
private:
	std::string name;
	size_t id;
};

// [문제] 파일 "Dog천마리"에는 class Dog 객체 1000개가 저장되어있다.
// 파일은 binary mode이고 각 객체는 메모리 크기 그대로 string의 write 함수로 기록하였다.
// 모든 객체는 한번의 write 함수를 사용하여 기록하였다.
// Dog의 멤버는 위의 코드와 같다.
// 메모리에 모두 읽어 와라.

//--------
int main()
//--------
{

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 15:04:41.3220621
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"

class Dog {
private:
	std::string name;
	size_t id;

	friend std::istream& operator >> (std::istream& is, const Dog& dog) {
		is.read((char*)&dog, sizeof(Dog));
		return is;
	}
};

// [문제] 파일 "Dog천마리"에는 class Dog 객체 1000개가 저장되어있다.
// 파일은 binary mode이고 각 객체는 메모리 크기 그대로 string의 write 함수로 기록하였다.
// 모든 객체는 한번의 write 함수를 사용하여 기록하였다.
// Dog의 멤버는 위의 코드와 같다.
// 메모리에 모두 읽어 와라.

//--------
int main()
//--------
{
	std::ifstream in{ "Dog천마리", std::ios::binary };
	if (not in) {
		std::cout << "Dog 없음" << std::endl;
		return 20260323;
	}

	Dog dog;
	size_t cnt{};
	while (in >> dog) {
		++cnt;
	}

	std::cout << cnt << std::endl;

	save("메인.cpp");
}