//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

int main()
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostreambuf_iterator<char>{out});
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

int main()
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

void save(const std::string& fname)
{
	std::ifstream in{ fname };
	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app };

	std::copy(std::istreambuf_iterator<char>{in}, {},
		std::ostreambuf_iterator<char>{out});
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	//std::copy(std::istreambuf_iterator<char>{in}, {},
	//	std::ostreambuf_iterator<char>{out});
	char c;
	in >> std::noskipws;
	while (in >> c)
		out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;
}﻿//-------------------------------------------------------------------//2026년1학기STL월56화783월3일(1주1일)//-------------------------------------------------------------------//컴파일환경-Release/x64//VS버전-17.14.27//프로젝트설정-C++언어표준-/std:c++lastest//-C/C++언어-SDL검사-아니요//-------------------------------------------------------------------//-------------------------------------------------------------------#include<iostream>#include<string>#include<fstream>#include<algorithm>voidsave(conststd::string&);//사용하지말아야할자료형->대체할수있다.->zero-overhead:대체품을사용함으로써생기는불리함이없음//1.char*->std::string//char*name="2026년";//name글자수?????->모름//strlen(name);->외부의함수를이용해서만글자수확인가능//std::cout<<"글자수-"<<fname.length()<<'\n';//2.T*->smartpointer를사용할것//3.T[]->std::array를사용할것//--------intmain()//--------{std::cout<<"2026년3월3일"<<'\n';save("메인.cpp");}//namingconvension->변수이름작명법참고voidsave(conststd::string&fname){std::ifstreamin{fname};//RAII공부할것//fname파일이존재하는지확인if(notin){//!는읽기어렵기때문에not으로바꿔쓸것std::cout<<fname<<"-열수없습니다."<<'\n';exit(202630);}std::ofstreamout{"2026년1학기STL월56화78.txt",std::ios::app};//std::ios::app이전내용에추가하는코드//std::copy(어디에서,어디까지를,어디로);std::copy(std::istream_iterator<char>{in},{},std::ostream_iterator<char>{out});//charc;//in>>std::noskipws;//while(in>>c)//out<<c;//operator>>(in,c);//고수준입력명령어//std::cout<<c;}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});

	char c;
	in >> std::noskipws;
	while (in >> c)
		out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;
}﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	std::cout << now << '\n';

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:22:27.9791166
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:24:25.3456257
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - %Y  -%m  -%d   %H   %M   %S  
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << std::format("%Y년-%m월-%d일 %H시 %M분 %S분", now + 9h) << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - %Y  -%m  -%d   %H   %M   %S  
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << std::format("%Y년-%m월-%d일 %H시 %M분 %S분", now + 9h) << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:34:54.4559081
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 1일)
//-------------------------------------------------------------------
// 컴파일 환경 - Release / x64
// VS 버전 - 17.14.27
// 프로젝트 설정 - C++언어표준 - /std:c++lastest
//				 - C/C++언어 - SDL검사 - 아니요
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <chrono>

void save(const std::string&);

// 사용하지 말아야 할 자료형 -> 대체할 수 있다. -> zero-overhead : 대체품을 사용함으로써 생기는 불리함이 없음
// 1. char* -> std::string

// char* name = "2026년";
// name 글자 수 ? ? ? ? ? ->모름
// strlen(name); -> 외부의 함수를 이용해서만 글자 수 확인 가능
// std::cout << "글자수 -  " << fname.length() << '\n';

// 2. T*	-> smart pointer를 사용할 것
// 3. T[]	-> std::array를 사용할 것

//--------
int main()
//--------
{
	std::cout << "2026년 3월 3일" << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

void save(const std::string& fname) 
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;
	
	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:48:55.2395772
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:52:25.9764022
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:53:00.6643528
=====================================================================

//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------
#pragma once

void save(const std::string&);

=====================================================================
        ð  - 2026-03-09 14:53:00.6657703
=====================================================================

﻿//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <chrono>
#include "save.h"

void save(const std::string& fname)
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;

	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:53:00.6670266
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:55:26.0457619
=====================================================================

//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------
#pragma once

void save(const std::string&);

=====================================================================
        ð  - 2026-03-09 14:55:26.0468581
=====================================================================

﻿//-------------------------------------------------------------------
// 한 학기 강의를 저장하기 위한 함수
// 
// 2026/03/09
//-------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <chrono>
#include "save.h"

void save(const std::string& fname)
{
	std::ifstream in{ fname };	// RAII 공부할 것

	// fname 파일이 존재하는지 확인
	if (not in) { // !는 읽기 어렵기 때문에 not으로 바꿔쓸 것
		std::cout << fname << " - 열 수 없습니다." << '\n';
		exit(202630);
	}

	std::cout << "\"" << fname << "\"" << "를 저장합니다." << '\n';

	std::ofstream out{ "2026년 1학기 STL 월56화78.txt", std::ios::app }; // std::ios::app 이전 내용에 추가하는 코드

	// 시간정보를 얻는다.

	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now(); // time epoch로 부터 1초마다 증가한 tick 수
	auto now = std::chrono::system_clock::now(); // auto = 함수의 반환값으로 자동으로 변수의 자료형을 정해줌

	using namespace std::chrono_literals;
	//std::cout << now + 9h << '\n'; // 시간이 utc 기준이므로 한국 기준인 9시간을 추가로 더해줘야 함

	out << '\n' << '\n';
	out << "=====================================================================" << '\n';
	out << "저장한 시간 - " << now + 9h << '\n';
	out << "=====================================================================" << '\n';
	out << '\n';

	//char c;
	//in >> std::noskipws;
	//while (in >> c)
	//	out << c;

	//operator >> (in, c); // 고수준 입력 명령어 
	//std::cout << c;

	// std::copy(어디에서, 어디까지를, 어디로);
	std::copy(std::istreambuf_iterator<char>{in}, {}, std::ostream_iterator<char>{out});
}

=====================================================================
        ð  - 2026-03-09 14:55:26.0477932
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	save("save.h");
	save("save.cpp");
	save("메인.cpp");
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 14:58:44.5254920
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int* a, int* b);

//--------
int main()
//--------
{
	std::cout << "save파일을 분리 하였다." << '\n';
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

// naming convension -> 변수 이름 작명법 참고

=====================================================================
        ð  - 2026-03-09 15:07:28.5541600
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int* a, int* b);

//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

//
// 메모리
// 
//


=====================================================================
        ð  - 2026-03-09 15:08:19.0783693
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int* a, int* b);

int a{ 1 };
//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int  b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp = *a;
	*a = *b;
	*b = tmp;
}

//
// 메모리
// 
//


=====================================================================
        ð  - 2026-03-09 15:11:56.1215318
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int*, int*);

//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int tmp{ *a };
	*a = *b;
	*b = tmp;
}

=====================================================================
        ð  - 2026-03-09 15:12:29.6218516
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int*, int*);

//--------
int main()
//--------
{
	std::cout << "main - " << std::addressof(main) << std::endl;
	std::cout << "save - " << std::addressof(save) << std::endl;
	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	std::cout << "a 주소 " << std::addressof(a) << std::endl;

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int temp{ *a };
	*a = *b;
	*b = temp;
}

=====================================================================
        ð  - 2026-03-09 15:12:53.0890023
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int*, int*);

//--------
int main()
//--------
{	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(&a, &b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int* a, int* b)
{
	int temp{ *a };
	*a = *b;
	*b = temp;
}

=====================================================================
        ð  - 2026-03-09 15:14:05.6198487
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

void change(int&, int&);

//--------
int main()
//--------
{	
	// [문제] 의도대로 실행되게 하자
	int a{ 1 }, b{ 2 };

	// 여기에서 change를 호출하였다.
	change(a, b);

	std::cout << a << ", " << b << std::endl; // 2, 1 이라고 출력되어야 함
	
	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

=====================================================================
Save Time - 2026-03-09 16:47:45.6190340
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(1주 2일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"
//using namespace std; // 강의 시 가독성을 위해 사용, 나는 이렇게 코딩하지 말자

class Dog {
private:
	int num;
public:
	Dog(int i) {
		num = i;
	}

	operator int&() {
		return num;
	}
};

void change(int&, int&);

//--------
int main()
//--------
{
	// [문제] 의도대로 실행되게 하자
	{
		// using Dog = int; , #define Dog Int 사용하지 말기
		Dog a{ 1 }, b{ 2 }; // 살려보기, Dog는 클래스, 템플릿 공부하기
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

=====================================================================
Save Time - 2026-03-10 15:43:11.4496064
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// save 파일 분리
// template
//-------------------------------------------------------------------
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

class Dog; //

void change(int&, int&);
void change(Dog&, Dog&);

// 내가 쓴 답, 잘 작동 됨
//class Dog {
//private:
//	int num;
//public:
//	Dog(int i) {
//		num = i;
//	}
//
//	operator int&() {
//		return num;
//	}
//};

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {}

	operator int() {
		return num;
	}
private:
	int num;
};

//--------
int main()
//--------
{
	// [문제] 의도대로 실행되게 하자
	{
		// using Dog = int; , #define Dog Int 사용하지 말기
		Dog a{ 1 }, b{ 2 }; // 살려보기, Dog는 클래스, 템플릿 공부하기
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b)
{
	Dog temp{ a };
	a = b; // a.orperator = (b);
	b = temp;
}

=====================================================================
Save Time - 2026-03-10 16:06:09.7780263
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

class Dog; //

void change(int&, int&);
void change(Dog&, Dog&);

class Dog {
public:
	Dog() = default;
	Dog(int n) : num{ n } {}

	// 이 코드는 사용하지 말자
	//operator int() {
	//	return num;
	//}

private:
	int num;

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		return os << dog.num;
	}
};

//--------
int main()
//--------
{
	// [문제] 의도대로 실행되게 하자
	{
		// using Dog = int; , #define Dog Int 사용하지 말기
		Dog a{ 1 }, b{ 2 }; // 살려보기, Dog는 클래스, 템플릿 공부하기
		// Dog a = 1, b = 2; -> Dog a = Dog { (int) 1 }; 강제로 형변환 시킴, 위험할 수 있음
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;
	}

	save("메인.cpp");
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b)
{
	Dog temp{ a };
	a = b; // a.orperator = (b);
	b = temp;
}

=====================================================================
Save Time - 2026-03-10 16:46:41.3458408
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a, b;
	change(a, b);

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:47:09.7757364
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a, b;
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:47:32.3454679
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a = 1, b = 2;
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:47:47.8966570
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:48:45.6010671
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;

	//change("S", "T");
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:52:18.8578401
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
	std::cout << "찍어낸 코드" << std::endl;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;
	
	std::string S{ "2026년" }, T{ "3월10일" };
	change(S, T);
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:52:36.0851529
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
	std::cout << "찍어낸 코드" << std::endl;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;
	
	std::string S{ "2026년" }, T{ "3월10일" };
	change(S, T);
	std::cout << S << ' ' << T << std::endl;
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 16:54:00.9990394
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

// 템플릿 함수 선언과 정의를 분리할 수 없음
template <class T>
void change(T& p, T& q)
{
	std::cout << "이 문장을 보면 컴파일러가 열일한 줄 알아라." << '\n';
	T temp{ p };
	p = q;
	q = temp;
}

template <>
void change<double>(double& p, double& q)
{
	double temp{ p };
	p = q;
	q = temp;
	std::cout << "찍어낸 코드" << std::endl;
}

//--------
int main()
//--------
{
	double a{ 1 }, b{ 2 };
	change(a, b);
	std::cout << a << ' ' << b << std::endl;
	
	std::string S{ "2026년" }, T{ "3월10일" };
	change(S, T);
	std::cout << S << ' ' << T << std::endl;
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:06:45.0328923
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(T a, T b)
{
	return a + b;
}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년"), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:07:23.5788312
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(T a, T b)
{
	return a + b;
}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년 "), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:12:17.8282934
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;
}

//template <>
//std::string add<std::string>(const std::string& a, const std::string& b)
//{
//	return std::string(a + b); // RVO -> 따로 보기
//}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년 "), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:13:41.1508646
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// template
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;
}

template <>
std::string add<std::string>(const std::string& a, const std::string& b)
{
	return std::string(a + b); // RVO -> 따로 보기
}

//--------
int main()
//--------
{
	// 템플릿 함수 add를 한 개 만들어 실행디ㅗ게 하라.
	std::cout << add<int>(1, 2) << std::endl;
	std::cout << add<std::string>(std::string("2026년 "), std::string("3월 10일")) << std::endl;
	//std::cout << add("2026년"s, "3월 10일"s) << std::endl; -> s붙이는거 어떻게 하는지 모름

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:16:53.4370726
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:21:10.6356808
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:27:55.7170371
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:38:41.5939756
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid;

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 100; i++) {
		std::cout << uid(dre) << std::endl;;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:40:35.7202808
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 100; i++) {
		std::cout << uid(dre) << std::endl;;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:41:16.5987185
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::cout << uid(dre) << std::endl;;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:42:57.2020053
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::print("{:8}", uid(dre));
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:43:12.7404450
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::print("{:8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:43:38.6401403
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	
	for (int i = 0; i < 1000; i++) {
		std::print("{:<8}", uid(dre));
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:48:37.8633222
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a)
	{
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", a);
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:48:49.9016565
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a)
	{
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", a);
	}
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:51:02.2262098
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", a);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:51:31.8715058
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<10}", a);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:51:52.9897612
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:8}", a);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:27.0804536
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:32.6866039
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:39.6720049
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:52:43.9988861
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max = 0;
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:53:14.6878575
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max{ 0 };
	for (int& num : a) {
		if (num > max) max = num;
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:53:41.5644584
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max{ 0 };
	for (int& num : a) {
		if (num > max) {
			max = num;
		}
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 13:54:32.4079360
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int max{ 0 };
	for (int& num : a) {
		if (num > max) {
			max = num;
		}
	}
	std::cout << max << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:02:01.9583160
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int maxVal{ std::numeric_limits<int>::min() };
	for (int& num : a) {
		if (maxVal < num) {
			maxVal = num;
		}
	}
	std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:02:15.7446557
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int maxVal{ std::numeric_limits<int>::min() };
	for (int& num : a) {
		if (maxVal < num) {
			maxVal = num;
		}
	}

	std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:02:19.7686681
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
	int maxVal{ std::numeric_limits<int>::min() };
	for (int& num : a) {
		if (maxVal < num) {
			maxVal = num;
		}
	}

	std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:15:03.7011477
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	// 이미 정의되어 있는 함수를 굳이 코딩할 필요가 없음
	//int maxVal{ std::numeric_limits<int>::min() };
	//for (int& num : a) {
	//	if (maxVal < num) {
	//		maxVal = num;
	//	}
	//}
	//std::cout << "가장 큰 값은 - " << maxVal << std::endl;

	// 0점 자리 답
	//std::sort(std::begin(a), std::end(a));
	//std::cout << "가장 큰 값 - " << a[1000 - 1] << std::endl;

	// std::cout << "가장 큰 값 -" << std::max_element(a) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:19:59.7791069
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	int* p = std::max_element(std::begin(a), std::end(a)); // begin(a) == &a[0], end(a) == &a[1000]
	std::cout << "가장 큰 값 - " << *p << std::endl; // *p 역참조 연산자 dereference

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:24:04.4154538
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	int* p = std::max_element(std::begin(a), std::end(a)); // begin(a) == &a[0], end(a) == &a[1000] -> 하드 와이어드 코딩, 코드 수정 시 오류날 가능성이 생겨서 추천 안함
	std::cout << "가장 큰 값 - " << *p << std::endl; // *p 역참조 연산자 dereference

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:40:55.1834226
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ std::random_device{}()};
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	for (int& num : a) {
		std::print("{:<8}", num);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하라.
 
	int* p = std::max_element(std::begin(a), std::end(a)); // begin(a) == &a[0], end(a) == &a[1000] -> 하드 와이어드 코딩, 코드 수정 시 오류날 가능성이 생겨서 추천 안함
	std::cout << "가장 큰 값 - " << *p << std::endl; // *p 역참조 연산자 dereference

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:48:47.6783610
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	int* maxVal{ std::max_element(std::begin(a), std::end(a)) };
	int* minVal{ std::min_element(std::begin(a), std::end(a)) };

	std::cout << "가장 작은 값 - " << *minVal << " 가장 큰 값 - " << *maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:56:52.9196951
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << res.first << " 가장 큰 값 - " << res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:57:06.8044094
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *res.first << " 가장 큰 값 - " << *res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:57:27.0679188
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(res.first) << " 가장 큰 값 - " << *(res.second) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 14:57:48.2672523
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	
	std::pair<int*, int*> res = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(res.first) << " 가장 큰 값 - " << *(res.second) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:01:39.0029019
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	auto [minVal, maxVal] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << minVal << " 가장 큰 값 - " << maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:01:56.9564532
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	int a[1000];

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	auto [minVal, maxVal] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(minVal) << " 가장 큰 값 - " << *(maxVal) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:09:16.6061794
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int [1000] 이런 자료구조는 앞으로는 쓰지 말 것, (new int[1000]; 제외)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// array<T,N>
	
	//int a[1000]; // 자료형은 int[N]인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	// [문제] 가장 작은 값과 큰 값을 찾아 화면에 출력하라.
	// syntatic sugar 인 structured binding을 사용하면 pair를 이렇게 쓸 수 있다.
	auto [minVal, maxVal] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *(minVal) << " 가장 큰 값 - " << *(maxVal) << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:10:26.4816136
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	// [문제] 랜덤값을 갖는 int 1000개를 만들어라.
	// int [1000] 이런 자료구조는 앞으로는 쓰지 말 것, (new int[1000]; 제외)
	// 대체할 수 있는 더 좋은 것이 있기 때문 - 시간/공간의 zero-overhead
	// array<T,N>
	
	//int a[1000]; // 자료형은 int[N]인데, 이름위치가 비정상

	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
	}

	auto [minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:11:20.2966708
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	std::array<int, 1000> a;

	for (int& num : a) {
		num = uid(dre);
		std::print("{:8}", num);
	}

	auto [minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:12:50.6546852
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	std::array<int, 1000> a;
	std::cout << "a가 사용하는 메모리 - " << sizeof a << std::endl;

	for (int& num : a) {
		num = uid(dre);
		std::print("{:8}", num);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(std::begin(a), std::end(a));
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:13:11.4952251
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

//--------
int main()
//--------
{
	std::array<int, 1000> a;
	std::cout << "a가 사용하는 메모리 - " << sizeof a << std::endl;

	for (int& num : a) {
		num = uid(dre);
		std::print("{:8}", num);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:19:59.1551807
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

template <class T, int N>
class myArray {
public:
	size_t size() const{
		return N;
	}

	int& operator[](int idx) {
		return a[idx];
	}
	T* begin() {
		return &a[0];
	}
	T* end() {
		return &a[N];
	}

private:
	T a[N];
};

//--------
int main()
//--------
{
	myArray<int, 1000> a;

	//for (int& num : a) {
	//	num = uid(dre);
	//	std::print("{:8}", num);
	//}

	for (int i = 0; i < a.size(); ++i) {
		a[i] = uid(dre);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-16 15:20:27.5968804
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 2일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>	// c++23 기능
#include <algorithm>
#include <array>	// C++11 부터 사용 가능
#include "save.h"

// MT19937 
std::default_random_engine dre{ 0 };
std::uniform_int_distribution uid{ 0, 9999 };

template <class T, int N>
class myArray {
public:
	size_t size() const{
		return N;
	}

	int& operator[](int idx) {
		return a[idx];
	}
	T* begin() {
		return &a[0];
	}
	T* end() {
		return &a[N];
	}

private:
	T a[N];
};

//--------
int main()
//--------
{
	myArray<int, 1000> a;

	//for (int& num : a) {
	//	num = uid(dre);
	//	std::print("{:8}", num);
	//}

	for (int i = 0; i < a.size(); ++i) {
		a[i] = uid(dre);
		std::print("{:8}", a[i]);
	}

	std::cout << std::endl;

	auto [minPos, maxPos] = std::minmax_element(a.begin(), a.end());
	std::cout << "가장 작은 값 - " << *minPos << " 가장 큰 값 - " << *maxPos << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 15:54:42.1336413
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:02:17.8048984
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << ' ';
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:02:57.0603632
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 100; ++i) {
		out << uid(dre) << " ";
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:05:03.5589246
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 1000'0000; ++i) {
		out << uid(dre) << std::endl;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:07:14.2638526
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 100; ++i) { // 119818581 byte
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:08:20.4290099
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <random>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{std::numeric_limits<int>::min(), std::numeric_limits<int>::max()};

//--------
int main()
//--------
{
	// [문제] 랜덤 int값 천만개를 다음과 같이 "int천만개.txt" 파일에 저장하였다.
	
	std::ofstream out{ "int천만개.txt" };
	
	for (int i = 0; i < 1000'0000; ++i) { // 119818581 byte
		out << uid(dre) << '\n';
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:40:01.1088842
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include <fstream>
#include "save.h"

std::array<int, 1000'1000> a;

//--------
int main()
//--------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자.
	// 가장 작은 값을 찾아 출력하라.
	
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요." << std::endl;
		return 20260317;
	}

	for (int& num : a) {
		in >> num;
	}

	std::cout << *(std::min_element(a.begin(), a.end()));
	
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-17 16:52:32.1271913
=====================================================================

//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(3주 1일)
//-------------------------------------------------------------------
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include <algorithm>
#include <fstream>
#include "save.h"

//--------
int main()
//--------
{
	// [문제] "int천만개.txt" 파일에는 int값 천만개가 저장되어있다.
	// 천만개가 있다는 사실은 틀림없다 - 믿고 쓰자.
	// 가장 작은 값을 찾아 출력하라.
	
	std::ifstream in{ "int천만개.txt" };
	if (not in) {
		std::cout << "파일 확인해 보세요." << std::endl;
		return 20260317;
	}

	std::cout << "최솟값 - " << *(std::min_element(std::istream_iterator<int>{in}, {})) << std::endl;
	// stream 데이터의 흐름, 

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:12:49.2920908
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}


	//std::pair <int, int> res = std::minmax_element();

	//std::cout << "최솟값 - " << res.first << " 최댓값 - " << res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:20:11.3292195
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

std::array<int, 10000000> a;

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}

	in.read((char*)(a.data()), a.size() * sizeof(int));

	auto [minVal, maxVal] = std::minmax_element(a.begin(), a.end());

	std::cout << "최솟값 - " << *minVal << " 최댓값 - " << *maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:12:49.2920908
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}


	//std::pair <int, int> res = std::minmax_element();

	//std::cout << "최솟값 - " << res.first << " 최댓값 - " << res.second << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:20:11.3292195
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
#include "save.h"

// [문제] 파일 "int천만개"에는 int값 천만개가 저장되어있다.
// 파일은 binary 모드로 열었고 int 값은 stream의 write함수를 사용하여 저장하였다.
// 저장된 int값을 모두 메모리에 저장하라.
// 저장된 값 중에서 가장 작은 값과 큰 값을 화면에 출력하라.

std::array<int, 10000000> a;

//--------
int main()
//--------
{
	std::ifstream in{ "int천만개", std::ios::binary };
	if (not in) {
		std::cout << "파일에 문제가 있다." << std::endl;
		return 20260323;
	}

	in.read((char*)(a.data()), a.size() * sizeof(int));

	auto [minVal, maxVal] = std::minmax_element(a.begin(), a.end());

	std::cout << "최솟값 - " << *minVal << " 최댓값 - " << *maxVal << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:16:53.4370726
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-10 17:21:10.6356808
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 3일				(2주 1일)
//-------------------------------------------------------------------
// C++에서 template의 역할은 무엇인가? - Generic Programming 구현하는 핵심 키워드
// C++언어의 paradigm
// 
// 다음 주 - 많은 수의 자료 다루기
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:46:42.8946179
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;			// [0, 9999]

	friend std::ostream& operator << (std::ostream & os, const Dog & dog) {
		print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{
	Dog dog;

	std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:48:17.4941733
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;			// [0, 9999]

	friend std::ostream& operator << (std::ostream & os, const Dog & dog) {
		print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{
	std::array <Dog, 1000> dogs;
	
	for (Dog dog : dogs)
	{
		std::cout << dog << std::endl;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:54:33.0096161
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i) {
			name += uidChar(dre);
		}
	}
private:
	std::string name;	// [10, 30] 사이의 소문자로 구성된 이름
	size_t id;			// [0, 9999]

	friend std::ostream& operator << (std::ostream & os, const Dog & dog) {
		print(os, "[{:>4}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{
	std::array <Dog, 1000> dogs;
	
	std::ofstream out{ "Dog천마리", std::ios::binary };
	out.write((char*)(dogs.data()), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 14:59:02.4700953
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"

class Dog {
private:
	std::string name;
	size_t id;
};

// [문제] 파일 "Dog천마리"에는 class Dog 객체 1000개가 저장되어있다.
// 파일은 binary mode이고 각 객체는 메모리 크기 그대로 string의 write 함수로 기록하였다.
// 모든 객체는 한번의 write 함수를 사용하여 기록하였다.
// Dog의 멤버는 위의 코드와 같다.
// 메모리에 모두 읽어 와라.

//--------
int main()
//--------
{

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-23 15:04:41.3220621
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 23일				(3주 2일)
//-------------------------------------------------------------------
// 많은 수의 자료를 다루기 - FILE I/O - binary I/O - class 객체
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include "save.h"

class Dog {
private:
	std::string name;
	size_t id;

	friend std::istream& operator >> (std::istream& is, const Dog& dog) {
		is.read((char*)&dog, sizeof(Dog));
		return is;
	}
};

// [문제] 파일 "Dog천마리"에는 class Dog 객체 1000개가 저장되어있다.
// 파일은 binary mode이고 각 객체는 메모리 크기 그대로 string의 write 함수로 기록하였다.
// 모든 객체는 한번의 write 함수를 사용하여 기록하였다.
// Dog의 멤버는 위의 코드와 같다.
// 메모리에 모두 읽어 와라.

//--------
int main()
//--------
{
	std::ifstream in{ "Dog천마리", std::ios::binary };
	if (not in) {
		std::cout << "Dog 없음" << std::endl;
		return 20260323;
	}

	Dog dog;
	size_t cnt{};
	while (in >> dog) {
		++cnt;
	}

	std::cout << cnt << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:13:57.9616388
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	int num;
	std::cout << "수를 입력하시오: ";
	std::cin >> num;
	
	int* ptr = new int[num];

	for (int i{ 0 }; i < num; i++) {
		ptr[i] = i + 1;
	}

	int sum{ 0 };
	for (int i{ 0 }; i < num; i++) {
		sum += ptr[i];
	}

	std::cout << sum << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:14:13.8102886
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	int num;
	std::cout << "수를 입력하시오: ";
	std::cin >> num;
	
	int* ptr = new int[num];

	for (int i{ 0 }; i < num; i++) {
		ptr[i] = i + 1;
	}

	int sum{ 0 };
	for (int i{ 0 }; i < num; i++) {
		sum += ptr[i];
	}

	std::cout << sum << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:15:03.7138759
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	int num;
	std::cout << "수를 입력하시오: ";
	std::cin >> num;
	
	int* ptr = new int[num];

	for (int i{ 0 }; i < num; i++) {
		ptr[i] = i + 1;
	}

	int sum{ 0 };
	for (int i{ 0 }; i < num; i++) {
		sum += ptr[i];
	}

	std::cout << sum << std::endl;

	delete[] ptr;
	ptr = nullptr;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:19:20.1031994
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	std::cout << "int 몇 개가 필요하신지? ";
	size_t num;
	std::cin >> num;

	if (num < 0) {
		std::cout << "음수는 안되요" << std::endl;
		return 20260324;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:20:03.6162699
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	std::cout << "int 몇 개가 필요하신지? ";
	size_t num;
	std::cin >> num;
	std::cout << num << std::endl;
	if (num < 0) {
		std::cout << "음수는 안되요" << std::endl;
		return 20260324;
	}

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:39:51.1179314
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	std::cout << "int 몇 개가 필요하신지? ";
	size_t num; // 음수를 입력할 경우 내부에서 이상한 값을 가지게 됨, 음수 넣으면 안됨
	std::cin >> num;
	
	// 설마 음수를 입력하지는 않으리라는 강력한 희망을!!

	int* p;
	try {
		p = new int[num]; // 게임루프에서 사용하기 부적합, 메모리를 잡는 타이밍에 써야 함, 실패할 경우 예외를 던지고 뻗는다.
	}
	catch (std::exception& e) {
		std::cout << "메모리가 고갈 - " << e.what() << std::endl;
	}

	for (int i = 0; i < num; ++i) {
		p[i] = i + 1;
	}

	int sum{ 0 };
	for (int i = 0; i < num; ++i) {
		sum += p[i];
	}

	std::cout << "1 부터 " << num << "까지의 합계 ----- " << sum << std::endl;

	delete[] p;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-24 16:39:58.6574971
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 24일				(4주 1 일)
//-------------------------------------------------------------------
// 동적 메모리 할당 (Dynamic Memory allocation)
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] 사용자가 입력한 수만큼 int를 저장할 메모리를 확보하라.
// 1부터 시작하는 정수로 메모리를 채워라.
// 합계를 화면에 출력하라.

//--------
int main()
//--------
{
	std::cout << "int 몇 개가 필요하신지? ";
	size_t num; // 음수를 입력할 경우 내부에서 이상한 값을 가지게 됨, 음수 넣으면 안됨
	std::cin >> num;
	
	// 설마 음수를 입력하지는 않으리라는 강력한 희망을!!

	int* p;
	try {
		p = new int[num]; // 게임루프에서 사용하기 부적합, 메모리를 잡는 타이밍에 써야 함, 실패할 경우 예외를 던지고 뻗는다.
	}
	catch (std::exception& e) {
		std::cout << "메모리가 고갈 - " << e.what() << std::endl;
	}

	for (int i = 0; i < num; ++i) {
		p[i] = i + 1;
	}

	int sum{ 0 };
	for (int i = 0; i < num; ++i) {
		sum += p[i];
	}

	std::cout << "1 부터 " << num << "까지의 합계 ----- " << sum << std::endl;

	delete[] p;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-30 13:42:43.4846408
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{
	int* p;
	
	p = new int{ 20260330 };

	std::cout << *p << std::endl;

	delete p;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-30 13:56:04.8098765
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

//--------
int main()
//--------
{	
	save("메인.cpp");

	Dog *p = new Dog;

	delete p;
}

=====================================================================
Save Time - 2026-03-30 13:56:18.5939365
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

//--------
int main()
//--------
{	
	save("메인.cpp");

	Dog *p = new Dog;
}

=====================================================================
Save Time - 2026-03-30 13:59:16.4819988
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

//--------
int main()
//--------
{	
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	std::unique_ptr<Dog> p{ new Dog };	// RAII

	return 0;
}

=====================================================================
Save Time - 2026-03-30 14:00:01.2774911
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

//--------
int main()
//--------
{	
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	std::unique_ptr<Dog> p{ new Dog };	// RAII

	return 0;

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

=====================================================================
Save Time - 2026-03-30 14:00:36.1307699
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

//--------
int main()
//--------
{	
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	std::unique_ptr<Dog> p{ new Dog };	// RAII

	return 0;

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

=====================================================================
Save Time - 2026-03-30 14:08:36.9599692
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)
// stack-unwinding

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

class 스마트포인터 {
public:
	스마트포인터(Dog* p) : p{ p } {}
	~스마트포인터() {
		delete p;
	}
private:
	Dog* p;
};

//--------
int main()
//--------
{	
	save("메인.cpp");

	// T* 는 raw-pointer이다. 앞으로 쓰지 말자

	 스마트포인터 p{ new Dog };	// RAII

	return 0;

	std::cout << "이 문장이 출력되면 노트북을 버려라" << std::endl;
}

=====================================================================
Save Time - 2026-03-30 14:11:09.7972608
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// 스마트 포인터 -> 콜러블 타입 -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <memory>
#include "save.h"

// c++ 언어의 보증사항
// 프로그램이 정상 종료시(return) 스택에 생긴 지역객체를 반드시 소멸됨을 보증한다.
// 비정상 종료(exit)
// stack-unwinding

class Dog {
public:
	Dog() { std::cout << "생성" << std::endl; }
	~Dog() { std::cout << "소멸" << std::endl; }
};

class 스마트포인터 {
public:
	스마트포인터(Dog* p) : p{ p } {}
	~스마트포인터() {
		delete p;
	}
private:
	Dog* p;
};

//--------
int main()
//--------
{	
	std::unique_ptr<Dog[]> p{new Dog[10]};

	save("메인.cpp");

	return 0;
}

=====================================================================
Save Time - 2026-03-30 14:15:45.6128708
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라

//--------
int main()
//--------
{	
	

	save("메인.cpp");

	return 0;
}

=====================================================================
Save Time - 2026-03-30 14:34:56.7298091
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <string>
#include "save.h"

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라

//--------
int main()
//--------
{	
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		std::cout << "열 수 없습니다." << std::endl;
		exit(20260330);
	}

	std::ofstream out{ "메인대문자.cpp" };

	char ch;
	
	while (in.get(ch)) {
		out.put(toupper(ch));
	}

	save("메인.cpp");

	return 0;
}

=====================================================================
Save Time - 2026-03-30 14:48:17.2797204
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

char 소투대(char c)
{
	return c = toupper(c);
}

//--------
int main()
//--------
{	
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	//transform(뭐를->메인.cpp, 뭘로->메인대문자.cpp, 어떻게->소문자를 대문자로);
	std::transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, 소투대);

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-30 14:50:15.8130880
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

//--------
int main()
//--------
{	
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	//transform(뭐를->메인.cpp, 뭘로->메인대문자.cpp, 어떻게->소문자를 대문자로);
	std::transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, [](char c) {
		return c = toupper(c);
		});

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-30 14:50:33.6834066
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <fstream>
#include <algorithm>
#include "save.h"

//--------
int main()
//--------
{	
	std::ifstream in{ "메인.cpp" };
	if (not in) {
		return 20260330;
	}

	std::ofstream out{ "메인대문자.cpp" };

	//transform(뭐를->메인.cpp, 뭘로->메인대문자.cpp, 어떻게->소문자를 대문자로);
	std::transform(std::istreambuf_iterator<char>{in}, {}, std::ostreambuf_iterator<char>{out}, 
		[](char c) {
			return c = toupper(c);
		});

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-30 15:14:55.3832221
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <print>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	for (int& num : arr) {
		num = uid(dre);
	}

	// 시간 측정 시작

	qsort(arr.data(), arr.size(), sizeof(std::array<int, 10000000>::value_type), 오름차순 ); // qsort는 []를 인식못함

	// 시간 측정 끝

	std::cout << "정렬 후 출력" << std::endl;
	for (int& num : arr) {
		std::print("{:8}", num);
	}
}

=====================================================================
Save Time - 2026-03-30 15:19:24.5789352
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <print>
#include <ranges>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	for (int& num : arr) {
		num = uid(dre);
	}

	// 시간 측정 시작

	qsort(arr.data(), arr.size(), sizeof(std::array<int, 10000000>::value_type), 오름차순 ); // qsort는 []를 인식못함

	// 시간 측정 끝

	std::cout << "정렬 후 출력" << std::endl;
	for (int& num : arr | std::views::take(1000)) {
		std::print("{:8}", num);
	}
	
}

=====================================================================
Save Time - 2026-03-30 15:20:25.2950745
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 30일			(4주 2일)
//-------------------------------------------------------------------
// callable type -> 실습 -> STL
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <print>
#include <ranges>
#include "save.h"

// [문제] 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// qsort를 이용하여 오름차순 정렬하라.
// 정렬결과를 앞에서부터 1000개만 화면에 출력하라.

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	for (int& num : arr) {
		num = uid(dre);
	}

	// 시간 측정 시작

	qsort(arr.data(), arr.size(), sizeof(std::array<int, 10000000>::value_type), 오름차순 ); // qsort는 []를 인식못함

	// 시간 측정 끝

	std::cout << "정렬 후 출력" << std::endl;
	for (int& num : arr | std::views::reverse
						| std::views::take(1000)) {
		std::print("{:8}", num);
	}
	
}

=====================================================================
Save Time - 2026-03-31 15:38:49.6565280
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - 
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	for (int& num : arr) {
		num = uid(dre);
	}

	// 시간 측정 시작
	auto start = std::chrono::high_resolution_clock::now();

	qsort(arr.data(), arr.size(), sizeof(int), 오름차순 );

	// 시간 측정 끝
	auto stop = std::chrono::high_resolution_clock::now();
	
	std::cout << "걸린시간 - " << stop - start << std::endl;
}

=====================================================================
Save Time - 2026-03-31 15:41:37.4661042
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - 
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	for (int& num : arr) {
		num = uid(dre);
	}

	// 시간 측정 시작
	auto start = std::chrono::high_resolution_clock::now();

	qsort(arr.data(), arr.size(), sizeof(int), 오름차순 );

	// 시간 측정 끝
	auto stop = std::chrono::high_resolution_clock::now();
	
	auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
	std::cout << "걸린시간 - " << duration << std::endl;
}

=====================================================================
Save Time - 2026-03-31 15:42:55.6165765
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - 
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	for (int& num : arr) {
		num = uid(dre);
	}

	// 시간 측정 시작
	auto start = std::chrono::high_resolution_clock::now();

	qsort(arr.data(), arr.size(), sizeof(int), 오름차순 );

	// 시간 측정 끝
	auto stop = std::chrono::high_resolution_clock::now();
	
	auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
	std::cout << "걸린시간 - " << duration << std::endl;
}

=====================================================================
Save Time - 2026-03-31 16:09:48.7992746
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - 
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

bool sort용오름차순(int a, int b)
{
	return a < b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	{
		for (int& num : arr) {
			num = uid(dre);
		}

		// 시간 측정 시작
		auto start = std::chrono::high_resolution_clock::now();

		qsort(arr.data(), arr.size(), sizeof(int), 오름차순);

		// 시간 측정 끝
		auto stop = std::chrono::high_resolution_clock::now();

		auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
		std::cout << "qsort 걸린시간 - " << duration << std::endl;
	}

	{
		for (int& num : arr) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now();

		std::sort(arr.begin(), arr.end(), sort용오름차순);

		auto stop = std::chrono::high_resolution_clock::now();

		auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
		std::cout << "sort 걸린시간 - " << duration << std::endl;
	}
}

=====================================================================
Save Time - 2026-03-31 16:10:28.0980732
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - 
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

bool sort용오름차순(int a, int b)
{
	return a < b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	{
		for (int& num : arr) {
			num = uid(dre);
		}

		// 시간 측정 시작
		auto start = std::chrono::high_resolution_clock::now();

		qsort(arr.data(), arr.size(), sizeof(int), 오름차순);

		// 시간 측정 끝
		auto stop = std::chrono::high_resolution_clock::now();

		auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
		std::cout << "qsort 걸린시간 - " << duration << std::endl;
	}

	{
		for (int& num : arr) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now();

		std::sort(arr.begin(), arr.end(), sort용오름차순);

		auto stop = std::chrono::high_resolution_clock::now();

		auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
		std::cout << "sort 걸린시간 - " << duration << std::endl;
	}
}

=====================================================================
Save Time - 2026-03-31 16:18:12.2177863
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - 
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999};

std::array<int, 10000000> arr;

int 오름차순(const void* a, const void* b)
{
	return *(int*)a - *(int*)b;
}

bool sort용오름차순(int a, int b)
{
	return a < b;
}

//--------
int main()
//--------
{	
	save("메인.cpp");

	{
		for (int& num : arr) {
			num = uid(dre);
		}

		// 시간 측정 시작
		auto start = std::chrono::high_resolution_clock::now();

		qsort(arr.data(), arr.size(), sizeof(int), 오름차순);

		// 시간 측정 끝
		auto stop = std::chrono::high_resolution_clock::now();

		auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
		std::cout << "qsort 걸린시간 - " << duration << std::endl;
	}

	{
		for (int& num : arr) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now();

		std::sort(arr.begin(), arr.end(), [](int a, int b) {
			return a < b;
			});

		auto stop = std::chrono::high_resolution_clock::now();

		auto duration = std::chrono::duration_cast<std::chrono::milliseconds>(stop - start);
		std::cout << "sort 걸린시간 - " << duration << std::endl;
	}
}

=====================================================================
Save Time - 2026-03-31 16:43:54.2419733
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

void f()
{
	std::cout << "난 호출 가능해요." << std::endl;
}

//--------
int main()
//--------
{	
	f();

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 16:46:50.6880095
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

void f()
{
	std::cout << "난 호출 가능해요." << std::endl;
}

//--------
int main()
//--------
{	
	int n; // addressof, sizeof, typeid

	f;

	std::cout << std::addressof(f) << std::endl;
	std::cout << typeid(f).name() << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 16:52:19.9221291
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

void f()
{
	std::cout << "난 호출 가능해요." << std::endl;
}

//--------
int main()
//--------
{	
	void (*a)() = f; // a는 함수 f를 가리키는 포인터
	
	a();

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 16:58:48.6744370
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
	std::cout << "점프" << std::endl;
}

void 슬라이드()
{
	std::cout << "슬라이드" << std::endl;
}

//--------
int main()
//--------
{	
	void (*왼쪽키)() = 점프;
	왼쪽키();
	왼쪽키();
	왼쪽키();

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 16:59:22.6387489
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
	std::cout << "점프" << std::endl;
}

void 슬라이드()
{
	std::cout << "슬라이드" << std::endl;
}

//--------
int main()
//--------
{	
	void (*왼쪽키)() = 점프;
	왼쪽키();
	왼쪽키();

	//설정에서 키 기능 변경
	왼쪽키 = 슬라이드;
	왼쪽키();

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:00:20.6036787
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

void 점프()
{
	std::cout << "점프" << std::endl;
}

void 슬라이드()
{
	std::cout << "슬라이드" << std::endl;
}

//--------
int main()
//--------
{	
	(*점프)();

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:04:13.3069892
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	[]() {};

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:04:44.0088073
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	[]() {
		std::cout << "안녕 난 람다야 반가워" << std::endl;
		};

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:05:47.0369301
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	[]() {
		std::cout << "안녕 난 람다야 반가워" << std::endl;
		}();

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:06:54.4474265
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	typeid([]() {
		std::cout << "안녕 난 람다야 반가워" << std::endl;
		});

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:07:11.5567326
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	typeid([]() {
		std::cout << "안녕 난 람다야 반가워" << std::endl;
		}());

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:08:18.2005855
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	std::cout << typeid([]() {
		std::cout << "안녕 난 람다야 반가워" << std::endl;
		}).name() << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:14:07.2865223
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

//--------
int main()
//--------
{	
	class Sakura {
	public:
		void operator()() { // function call operator
			std::cout << "안녕 난 람다야 반가워" << std::endl;
			};
	};

	Sakura a;
	a();

	std::cout << typeid(a).name() << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-03-31 17:15:16.4554629
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			3월 31일			(5주 1일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// 1. 함수
//  - 함수포인터도 호출 가능
// 2. lambda
//-------------------------------------------------------------------
#include <iostream>
#include "save.h"

class Sakura {
public:
	void operator()() { // function call operator
		std::cout << "안녕 난 람다야 반가워" << std::endl;
	};
};

//--------
int main()
//--------
{	
	Sakura a;
	a();

	std::cout << typeid(a).name() << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 13:34:39.3199267
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	std::sort(a.begin(), a.end());
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 13:36:51.8092050
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	std::sort(a.begin(), a.end(), 정렬기준);
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 13:38:34.7591718
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	//std::sort(a.begin(), a.end(), 정렬기준);
	std::sort(a.begin(), a.end(), [](int a, int b) {
		std::cout << "람다 ";
		return a < b;
		});
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 13:40:51.3357662
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog {
public:
	bool operator()(int a, int b) {
		std::cout << "Dog ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) {
	//	std::cout << "람다 ";
	//	return a < b;
	//	});
	std::sort(a.begin(), a.end(), Dog{});
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 13:41:02.2677209
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog {
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) {
	//	std::cout << "람다 ";
	//	return a < b;
	//	});
	std::sort(a.begin(), a.end(), Dog{});
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 13:45:21.4043002
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog {
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) {
	//	std::cout << "람다 ";
	//	return a < b;
	//	});
	std::sort(a.begin(), a.end(), Dog{});
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

//std::sort<RanIt>(a.begin(), a.end(), Callable f)
//{
//	f( *a.begin(), *(a.begin())+ 5))
//}

=====================================================================
Save Time - 2026-04-06 13:45:50.9280002
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog { // function object
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) {
	//	std::cout << "람다 ";
	//	return a < b;
	//	});
	std::sort(a.begin(), a.end(), Dog{});
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

//std::sort<RanIt>(a.begin(), a.end(), Callable f)
//{
//	f( *a.begin(), *(a.begin())+ 5))
//}

=====================================================================
Save Time - 2026-04-06 13:49:12.4974688
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog { // function object
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
auto main() -> int
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	//std::sort(a.begin(), a.end(), 정렬기준);
	std::sort(a.begin(), a.end(), [](int a, int b) -> bool { // 함수 선언문 제2 형식
		std::cout << "람다 ";
		return a < b; // 관계식을 리턴하기 때문에 컴파일러가 bool을 반환한다고 유추 가능, -> bool 생략 가능 
		});
	//std::sort(a.begin(), a.end(), Dog{});
	
	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

//std::sort<RanIt>(a.begin(), a.end(), Callable f)
//{
//	f( *a.begin(), *(a.begin())+ 5))
//}

=====================================================================
Save Time - 2026-04-06 14:00:16.1438765
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include <functional>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog { // function object
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	std::function<bool(int, int)> f;

	//f = 정렬기준;
	//f = [](int a, int b) -> bool{
	//	std::cout << "람다 ";
	//	return a < b;
	//	};
	f = Dog{};

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) -> bool { // 함수 선언문 제2 형식
	//	std::cout << "람다 ";
	//	return a < b; // 관계식을 리턴하기 때문에 컴파일러가 bool을 반환한다고 유추 가능, -> bool 생략 가능 
	//	});
	//std::sort(a.begin(), a.end(), Dog{});
	
	std::sort(a.begin(), a.end(), f);

	std::cout << std::endl;

	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

//std::sort<RanIt>(a.begin(), a.end(), Callable f)
//{
//	f( *a.begin(), *(a.begin())+ 5))
//}

=====================================================================
Save Time - 2026-04-06 14:00:47.8107840
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include <functional>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog { // function object
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	std::function<bool(int, int)> f;

	//f = 정렬기준;
	f = [](int a, int b) -> bool{
		std::cout << "람다 ";
		return a < b;
		};
	//f = Dog{};

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) -> bool { // 함수 선언문 제2 형식
	//	std::cout << "람다 ";
	//	return a < b; // 관계식을 리턴하기 때문에 컴파일러가 bool을 반환한다고 유추 가능, -> bool 생략 가능 
	//	});
	//std::sort(a.begin(), a.end(), Dog{});
	
	std::sort(a.begin(), a.end(), f);

	std::cout << std::endl;

	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

//std::sort<RanIt>(a.begin(), a.end(), Callable f)
//{
//	f( *a.begin(), *(a.begin())+ 5))
//}

=====================================================================
Save Time - 2026-04-06 14:00:57.0374419
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 호출가능타입 - callable types
// function을 사용하여 모든 호출가능 타입을 표현할 수 있다
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <algorithm>
#include <functional>
#include "save.h"

bool 정렬기준(int a, int b)
{
	std::cout << "함수 "; // nlogn -> 10 * (3 ~ 4)
	return a < b;
}

class Dog { // function object
public:
	bool operator()(int a, int b) {
		std::cout << "도그 ";
		return a < b;
	}
};

//--------
int main()
//--------
{	
	std::array<int, 10> a{ 8, 4, 2, 0, 1, 9, 7, 5, 6, 3 };

	std::function<bool(int, int)> f;

	f = 정렬기준;
	//f = [](int a, int b) -> bool{
	//	std::cout << "람다 ";
	//	return a < b;
	//	};
	//f = Dog{};

	//std::sort(a.begin(), a.end(), 정렬기준);
	//std::sort(a.begin(), a.end(), [](int a, int b) -> bool { // 함수 선언문 제2 형식
	//	std::cout << "람다 ";
	//	return a < b; // 관계식을 리턴하기 때문에 컴파일러가 bool을 반환한다고 유추 가능, -> bool 생략 가능 
	//	});
	//std::sort(a.begin(), a.end(), Dog{});
	
	std::sort(a.begin(), a.end(), f);

	std::cout << std::endl;

	for (int num : a)
		std::cout << num << ' ';
	std::cout << std::endl;

	save("메인.cpp");
}

//std::sort<RanIt>(a.begin(), a.end(), Callable f)
//{
//	f( *a.begin(), *(a.begin())+ 5))
//}

=====================================================================
Save Time - 2026-04-06 14:14:00.4879841
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <print>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		return os;
	}
};

//--------
int main()
//--------
{	
	for (int i = 0; i < 10; ++i)
		std::cout << Dog{} << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:48:05.2625780
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid(){
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	std::cout << "정렬시작";
	std::sort(Dogs.begin(), Dogs.end(), [](Dog a, Dog b) {
		return a.getid() < b.getid();
		});
	std::cout << "정렬끝" << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:48:22.2598041
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid(){
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	std::cout << "정렬시작" << std::endl;
	std::sort(Dogs.begin(), Dogs.end(), [](Dog a, Dog b) {
		return a.getid() < b.getid();
		});
	std::cout << "정렬끝" << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:51:27.0983851
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	std::sort(Dogs.begin(), Dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getid() < b.getid();
		});

	for (const Dog& dog : Dogs)
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:51:59.0856930
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		//print(os, "[{:7}] - {}", dog.id, dog.name);
		print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	std::sort(Dogs.begin(), Dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getid() < b.getid();
		});

	for (const Dog& dog : Dogs)
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:53:08.7222544
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	std::sort(Dogs.begin(), Dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getid() < b.getid();
		});

	for (const Dog& dog : Dogs | std::views::take(1000))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:53:26.9778237
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	std::sort(Dogs.begin(), Dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getid() < b.getid();
		});

	for (const Dog& dog : Dogs | std::views::take(1000))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 14:57:20.2930641
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 id 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> Dogs;

//--------
int main()
//--------
{	
	//std::sort(Dogs.begin(), Dogs.end(), [](const Dog& a, const Dog& b) {
	//	return a.getid() < b.getid();
	//	});

	// C++20의 sort
	std::ranges::sort(Dogs, {}, &Dog::getid);

	for (const Dog& dog : Dogs | std::views::take(1000))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:03:46.4370479
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 name의 길이 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	//std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
	//	return a.getid() < b.getid();
	//	});

	// C++20의 sort
	//std::ranges::sort(dogs, {}, &Dog::getid);

	std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getName().size() < b.getName().size();
		});

	for (const Dog& dog : dogs | std::views::take(1000))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:04:22.2990604
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// std::sort를 사용하여 name의 길이 기준 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	//std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
	//	return a.getid() < b.getid();
	//	});

	// C++20의 sort
	//std::ranges::sort(dogs, {}, &Dog::getid);

	std::sort(dogs.begin(), dogs.end(), [](const Dog& a, const Dog& b) {
		return a.getName().size() < b.getName().size();
		});

	for (const Dog& dog : dogs)
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:13:18.4583474
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// 각 Dog 객체의 이름을 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	for (Dog dog : dogs) {
		std::string& name = dog.getName();
		std::sort(name.begin(), name.end());
	}

	for (const Dog& dog : dogs | std::views::take(10))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:13:56.8902108
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// 각 Dog 객체의 이름을 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	for (Dog dog : dogs) {
		std::string& name = dog.getName();
		std::sort(name.begin(), name.end());
	}

	for (const Dog& dog : dogs | std::views::take(100))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:14:35.8782831
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// 각 Dog 객체의 이름을 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	for (Dog dog& : dogs) {
		std::string& name = dog.getName();
		std::sort(name.begin(), name.end());
	}

	for (const Dog& dog : dogs | std::views::take(100))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:14:43.1257015
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 150 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 150]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

// [문제] Dog 객체 10만개를 메모리(array)에 저장하라.
// 각 Dog 객체의 이름을 오름차순으로 정렬하라.
// 필요하다면 Dog에 interface 멤버를 추가하라.
// 앞에서 1000개의 내용을 cout으로 출력하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	for (Dog& dog : dogs) {
		std::string& name = dog.getName();
		std::sort(name.begin(), name.end());
	}

	for (const Dog& dog : dogs | std::views::take(100))
		std::cout << dog << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 15:17:55.6939521
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 16 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 16]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	std::ofstream out{ "Dog 십만마리", std::ios::binary };

	out.write((char*)dogs.data(), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 16:44:37.3723886
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 16 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 16]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{
	std::ofstream out{ "Dog 십만마리", std::ios::binary };

	out.write((char*)dogs.data(), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-06 16:44:52.3109139
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 6일			(5주 2일)
//-------------------------------------------------------------------
// 예제
//-------------------------------------------------------------------
#include <iostream>
#include <random>
#include <string>
#include <array>
#include <algorithm>
#include <ranges>
#include <fstream>
#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999999 };
std::uniform_int_distribution uidNameLen{ 1, 16 };
std::uniform_int_distribution<> uidChar{ '!', '~' };

class Dog {
public:
	Dog() {
		id = uid(dre);
		int len = uidNameLen(dre);
		for (int i = 0; i < len; ++i)
			name += uidChar(dre);
	}

	int getid() const {
		return id;
	}

	std::string getName() const {
		return name;
	}

	std::string& getName() {
		return name;
	}

private:
	std::string name;	// [1, 16]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		//print(os, "[{:7}]", dog.id);
		return os;
	}
};

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{
	std::ofstream out{ "Dog 십만마리", std::ios::binary };

	out.write((char*)(dogs.data()), dogs.size() * sizeof(Dog));

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 16:06:19.4689340
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// 중간고사 4월 21일 (8주 1일)
//-------------------------------------------------------------------
#include <iostream>
#include <array>
#include <fstream>
#include <ranges>
#include "save.h"

class Dog {
private:
	std::string name;	// [1, 16]
	int id;				// [0, 9999999]

	friend std::ostream& operator << (std::ostream& os, const Dog& dog) {
		print(os, "[{:7}] - {}", dog.id, dog.name);
		return os;
	}

	friend std::istream& operator >> (std::istream& is, const Dog& dog) {
		is.read((char*)&dog, sizeof(Dog));
		return is;
	}
};

// [문제] 다운받은 "Dog 십만마리"에는 class Dog 객체 십만개가 저장되어 있다.
// 바이너리 모드로 저장하여 정확하게 4MB이다.
// 메모리로 모두 읽어와라.
// 앞에서 100개 출력하여 확인하라.

std::array<Dog, 100000> dogs;

//--------
int main()
//--------
{	
	// 내가 문제 풀어본것
	save("메인.cpp");
	std::ifstream in{ "Dog 십만마리", std::ios::binary };

	if (not in) {
		std::cout << "파일이 없습니다." << std::endl;
		exit(20260406);
	}

	for (Dog dog : dogs) {
		in >> dog;
	}

	for (const Dog& dog : dogs | std::views::take(100)) {
		std::cout << dog << std::endl;
	}

	
}

=====================================================================
Save Time - 2026-04-07 16:39:28.4219138
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

//--------
int main()
//--------
{	
	std::string s{ "2026" };
	std::cout << s << std::endl;

	s += "0407";
	std::cout << s << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 16:41:04.1499022
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

//--------
int main()
//--------
{	
	std::string s{ "2026" };
	s += "0407";
	std::cout << s << std::endl;

	for (auto i{ s.begin() }; i < s.end(); ++i)
		std::cout << *i << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 16:41:52.7833260
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include "save.h"

//--------
int main()
//--------
{	
	std::string s{ "2026" };
	s += "0407";
	std::cout << s << std::endl;

	for (auto i{ s.rbegin() }; i < s.rend(); ++i)
		std::cout << *i << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 16:54:31.9926597
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() = default;
	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	ZString s{ "2026" };
	std::cout << s << std::endl;


	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 16:57:57.5108285
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() = default;
	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 16:58:31.0569787
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() = default;
	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 17:00:09.6613571
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() = default;
	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수: " << len << " 객체: " << this << " 글자주소: " << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 17:01:00.4853845
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() = default;
	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 17:02:49.0299627
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	};

	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	ZString{};

	ZString s{ "2026" };
	std::cout << s << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 17:12:42.8419405
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	};

	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	std::string s{ "2026년" };
	std::string t{ "4월 7일 화요일" };
	std::string u = s + t;
	std::cout << u << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 17:14:49.1797728
=====================================================================

﻿//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	};

	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	std::string s{ "2026년" };
	std::string t = move(s);
	std::cout << "s - " << s << std::endl;
	std::cout << "t - " << t << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 18:15:09.1591985
=====================================================================

﻿#define _CRT_SECURE_NO_WARNINGS
//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	};

	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

	friend ZString& operator += (const ZString& azs, ZString& zs) {
		std::unique_ptr<char[]> temp(new char[zs.len + azs.len]);
		memcpy(temp.get(), zs.p.get(), zs.len);
		std::cout << temp << std::endl;
		strncat(zs.p.get(), azs.p.get(), azs.len);
		zs.p = std::move(temp);
		return zs;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	// 이 동작 다시 설명하고 ZString으로 바꾸면 안되는 이유부터 시작

	//std::string s{ "2026년" };
	//std::string t = move(s);
	//std::cout << "s - " << s << std::endl;
	//std::cout << "t - " << t << std::endl;

	ZString a{ "2026" };
	std::cout << a << std::endl;
	ZString b{ "0407" };
	std::cout << b << std::endl;
	a += b;
	std::cout << a << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 18:15:36.6685070
=====================================================================

﻿#define _CRT_SECURE_NO_WARNINGS
//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	};

	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

	friend ZString& operator += (const ZString& azs, ZString& zs) {
		std::unique_ptr<char[]> temp(new char[zs.len + azs.len]);
		memcpy(temp.get(), zs.p.get(), zs.len);
		std::cout << temp << std::endl;
		strncat(zs.p.get(), azs.p.get(), azs.len);
		zs.p = std::move(temp);
		return zs;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	// 이 동작 다시 설명하고 ZString으로 바꾸면 안되는 이유부터 시작

	//std::string s{ "2026년" };
	//std::string t = move(s);
	//std::cout << "s - " << s << std::endl;
	//std::cout << "t - " << t << std::endl;

	ZString a{ "2026" };
	std::cout << a << std::endl;
	ZString b{ "0407" };
	std::cout << b << std::endl;
	a += b;
	std::cout << a << std::endl;

	save("메인.cpp");
}

=====================================================================
Save Time - 2026-04-07 18:15:58.8318046
=====================================================================

﻿#define _CRT_SECURE_NO_WARNINGS
//-------------------------------------------------------------------
// 2026년 1학기 STL 월56화78			4월 7일			(6주 1일)
//-------------------------------------------------------------------
// STL 컨테이너 - std::string과 유사한 ZString을 만들어서 컨테이너 본질에 접근
//-------------------------------------------------------------------
#include <iostream>
#include <string>
#include<memory>
#include "save.h"

class ZString {
public:
	ZString() {
		std::cout << "생성() 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	};

	ZString(const char* s) {
		len = strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get(), s, len);

		std::cout << "생성(char*) 글자수:" << len << " 객체:" << this << " 글자주소:" << (void*)p.get() << std::endl;
	}

	friend std::ostream& operator<<(std::ostream& os, const ZString& zs) {
		for (int i = 0; i < zs.len; ++i)
			os << *(zs.p.get() + i);
		return os;
	}

	friend ZString& operator += (const ZString& azs, ZString& zs) {
		std::unique_ptr<char[]> temp(new char[zs.len + azs.len]);
		memcpy(temp.get(), zs.p.get(), zs.len);
		std::cout << "temp : " << temp << std::endl;
		strncat(zs.p.get(), azs.p.get(), azs.len);
		zs.p = std::move(temp);
		return zs;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};
};

//--------
int main()
//--------
{	
	// 이 동작 다시 설명하고 ZString으로 바꾸면 안되는 이유부터 시작

	//std::string s{ "2026년" };
	//std::string t = move(s);
	//std::cout << "s - " << s << std::endl;
	//std::cout << "t - " << t << std::endl;

	ZString a{ "2026" };
	std::cout << a << std::endl;
	ZString b{ "0407" };
	std::cout << b << std::endl;
	a += b;
	std::cout << a << std::endl;

	save("메인.cpp");
}