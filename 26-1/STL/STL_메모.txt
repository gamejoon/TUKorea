26/03/03
C++ 과목임
출석 점수 없음, 1/4 이상만 결석하지 않기
사진 및 동영상 촬영, 음성 녹음 가능
딴 짓 하다가 교수 심기 건드리면 수업 안하고 배쨀거다 선언
학점 ㅈㄴ 빡세게 줄거다, 지난 학기 수업 F 40% 이상
시험때는 수업 안들었거나, gpt 등 ai는 못풀도록 만들 예정
수업시간에 모르는 문제같은 것은 교수 메일로 보내면 며칠 내로 답해줄것이다 (메일 : wulong@tukorea.ac.kr)
C++ 복습하기

C++ -> Object Oriented P/L 객체지향 프로그래밍 언어
	   유저가 자유롭게 정의할 수 있음

class NPC { 		-> coding convention
			-> modeling
attributes; <- variable 변수
behaviors; <- function 함수
};

NPC a; -> instancing -> { STACK, DATA, free-store(HEAP) } 영역 중 한 곳에 존재

NPC <-> MONSTER <-> PLAYER <-> NPC 객체들간의 관계

매우 특수한 관계 : Inheritance 상속

상속-	1. code reuse 코드 재활용
	2. polymorphism 다형성 구현

C++ - OOP 	virtual - vptr 가상함수 테이블을 위한 포인터가 무조건 할당됨
ㄴ> 문제를 해결하기 위한 방법 

C++ STL -> Standard Template Library
Template -> General Programming -> Generic 자료형과 관계없는! -> 함수 => algorithm     --- } template
											  -> 클래스 => container     /
참고자료는 https://www.cppreference.com 참고
deque -> 디큐 아님 뎈 라고 읽음
Iterators 23개의 디자인 패턴 중 하나 -> 자료구조와 알고리즘을 엮어주는 본드 역할, 중요

과제 예제
1. 게임 세이브 파일 세이브, 로드 구현
2. 수천만개의 데이터를 정렬하여 원하는 데이터 검색 및 속도
3. 1억개 이상의 정수를 합산하여 그 답이 얼마나 정확한지?

시험은 완전 오픈북 -> ㅈㄴ 어렵다


26/03/09
시험때 새로운 빈프로젝트를 생성할때 컴파일러 버전 꼭 최신버전으로 변경할 것

26/03/10
알고리즘 절차 -> 자료형에 관계 없이 절차만 지키면 문제 없이 실행되는 것
save.h 와 save.cpp를 나누는 이유 -> (정의)save.cpp를 감추고 싶어서 (선언)save.h만 보여주기 위해
템플릿은 찍어내기 위해서 정의를 감출 수 없음
() 콜러블 펑션 - 뭘 호출해야 하나 - 호출 우선순위 결정
템플릿 추론 template deduction
double에대한 specialization
템플릿의 단점 : 오류가 생겼을 때 문제가 생긴 부분을 찾기 어렵다 ex) 1줄 틀려서 에러폭풍
템플릿의 장점 : 나머지 전부 다, 코드의 재사용성과 성능이 증가

26/03/16
MT19937은 랜덤값을 뽑는 엔진
class ExecutionPolicy 병렬처리, cppreference 참조
std::random_device{}() 랜덤으로 데이터를 출력할 수 있게 함.
syntatic sugar 찾아볼것


26/03/17
int, not, true, do 등등 -> C++이 미리 사용하겠다고 정해놓은 keyword
사용자가 정의하여 사용 할 수 있는 것 -> identfiy

X(자료형) Y(이름);
예외 
int a[1000]; -> 자료형 : int [1000], 이름 : a
함수포인터 -> void (*f)(string) 

템플릿에 전달되는 인자
타입 인자, 비타입 인자, 템플릿 인자

백준 꾸준히 풀것

in >> num; -> 고급 입력, operator >> (in, num)

MB -> B -> 바이트 (저장 용량 사용), Mb -> b -> 비트 데이터 (전송 속도 사용)

// [문제] int로 표현할 수 있는 값은 -2147483648(-2^31) 부터 2147483647(2^31 - 1) 까지 이다.
// 모든 int값을 하나도 빼지 않고 한 개씩 파일에 ㄱ text로 기록하였다.
// 값과 값은 빈칸 한 개로 구분하였다.
// 이렇게 하면 int를 한 개 기록하는데 평균 몇 바이트가 필요한지 계산하라.
위 문제를 ai로 돌렸지만 정확히 풀 지 못했다.


26/03/24
콜러블타입 찾아볼것

26/03/30
동적 할당받은 자원을 실수로 두 번 반환시키면 프로그램이 즉사한다.
혹은 자원을 반환시키기 이전에 프로그램이 강제로 종료될 경우 할당받은 자원을 반환시키지 못하고 메모리가 누수가 생긴다.
동사 = 알고리즘

// [문제] "메인.cpp"의 소문자를 모두 대문자로 바꾼 "메인대문자.cpp"를 만들어라
transform(뭐를 -> 메인.cpp, 뭘로 -> 메인대문자.cpp, 어떻게 -> 소문자를 대문자로);

std::transform(메인시작, 메인끝, 메인대문자시작으로, 함수의 시작번지)
std::istreambuf_iterator<char>{} -> {}으로 줄임
람다 []
void*
| 버티컬바, 연산자 말고도 필터 역할로도 쓰임, 연속으로 사용 가능


26/03/31
qsort와 sort는 알고리즘 차이가 있기 때문에 sort가 아주 살짝 더 빠름
다음 주에 중간고사 대비 과제 있음


26/04/07
30점짜리 과제
os.write((char*)this, sizeof(Player)) 64바이트 그대로 기록
->플레이어의 정보 기록
os.write((char*)p.get(), num); num 바이트 수만큼 기록

3. 같은 아이디.txt에 기록
이름 id
이름 id
id로 오름차순으로 정렬 후 해도 되고, 그대로 해도 됨. 

5. 
찾는 id가 없을경우 없다고 출력
1 - id 기준 앞뒤
-> 동일한 id가 존재하는 경우 전부 출력

2 - name 기준 앞뒤

3 - 점수 기준 앞뒤

1,2,3,4번 결과 복붙하여 제출
5번 화면 캡쳐하여 제출, 콘솔화면 흰색으로 변경

개선할 부분 -> 내가 모자란 부분, 개선할 부분, 혹은 문제에 추가했으면 하는 요소 등등

보고서 인쇄하여 실물 제출
소스코드는 cpp파일을 eclass 과제 제출로 제출